(function ()
{
    'use strict';    
    angular
        .module('empApp', [
            /*
             * Inject your dependencies here
             * |
             * Core dependencies
             * |
             * |
             * Application dependencies
             * |
             */
        ])
})();